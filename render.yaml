services:
  - type: web
    name: military-asset-management
    env: node
    plan: free
    rootDirectory: military-asset-management
    buildCommand: "cd military-asset-management && npm install && npm run build && cd ../backend && npm install"
    startCommand: "cd backend && node index.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
    healthCheckPath: /health
